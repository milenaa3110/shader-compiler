uniform mat4 MVP;
out vec4 vColor;
out vec2 vUV;

@entry @stage(vertex)
fn void main() {
    float x = float(gl_VertexID);
    float y = float(gl_InstanceID);
    gl_Position = vec4(x * 0.1, y * 0.1, 0.0, 1.0);
    vColor = vec4(1.0, 0.5, 0.0, 1.0);
    vUV = vec2(x * 0.25, y * 0.25);
}